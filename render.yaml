services:
  - type: web
    name: rag-portfolio-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    port: 8000
    envVars:
      - key: GROQ_API_KEY
        scope: run
      - key: PYTHONUNBUFFERED
        value: "1"
    healthCheckPath: /health
    autoDeploy: false
